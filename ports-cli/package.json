{
  "name": "creative-plate-ports",
  "version": "0.1.0",
  "description": "A macOS-only CLI that shows currently listening TCP ports at a glance.",
  "type": "module",
  "bin": {
    "ports": "./bin/ports.js"
  },
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.js",
    "check": "npm test && npm run build"
  },
  "keywords": [
    "macos",
    "ports",
    "lsof",
    "cli"
  ],
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
