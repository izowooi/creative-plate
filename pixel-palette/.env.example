# Replicate API Token (server-side only, never expose to client)
REPLICATE_API_TOKEN=r8_xxxxxxxxxxxxxxxxxxxx

# App password for access control (exposed to client bundle - simple gate only)
NEXT_PUBLIC_APP_PASSWORD=your-password-here
