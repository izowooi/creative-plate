# youtube-to-srt 로컬 환경 설정 템플릿
#
# 사용법:
#   1) 이 파일을 복사: cp .env.local.example .env.local
#   2) .env.local 안에 실제 값 채우기
#   3) .env.local은 .gitignore 처리되어 절대 커밋되지 않음
#
# Cloudflare Zero Trust → Networks → Tunnels에서 토큰 발급 후 붙여넣기
CLOUDFLARE_TUNNEL_TOKEN=
